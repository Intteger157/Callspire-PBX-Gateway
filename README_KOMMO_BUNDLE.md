# Kommo upload — куда копировать на сервер

Полный готовый пакет для одноразового копирования:

**[`deploy/pbx-gateway-kommo-bundle/`](../../pbx-gateway-kommo-bundle/)**

Скопируйте всё содержимое этой папки в корень PBX Gateway на сервере (`/opt/mikopbx-cdr-proxy/` или аналог).

Правки `app.py` — см. `APP_PY_PATCH.md` в том же бандле (если не используете `app.py` из этой `gateway-patch` целиком).
